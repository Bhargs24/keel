# Mockups

Self-contained HTML mockups of the key screens (inline CSS, system or Google
fonts, no external assets), covering every state the PRD names - loading, empty,
partial, error, offline. Open one with a double-click. Built by the design-lead.
