# Module specs

One file per module (`M1.md`, `M2.md`, …), each at requirements depth: what it
does, the user stories it satisfies, every screen and every state, the actions,
the edge cases, the data it reads and writes, and its acceptance criteria.

Written by the product-manager. Template: `docs/99-TEMPLATES/prd-module.md`.
