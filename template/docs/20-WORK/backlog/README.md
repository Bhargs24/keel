# Backlog

One file per work item, the human-readable companion to the tracker task.
`/plan` creates the tracker tasks from the build roadmap; the detailed items
live here. Template: `docs/99-TEMPLATES/work-item.md`.
